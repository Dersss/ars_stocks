%h1 Listing cryptos

%table
  %thead
    %tr
      %th Name
      %th Symbol
      %th Price
      %th Day open
      %th Day close
      %th Minute open
      %th Minute close
      %th Day high
      %th Day low
      %th Minute high
      %th Minute low
      %th
      %th
      %th

  %tbody
    - @cryptos.each do |crypto|
      %tr
        %td= crypto.name
        %td= crypto.symbol
        %td= crypto.price
        %td= crypto.day_open
        %td= crypto.day_close
        %td= crypto.minute_open
        %td= crypto.minute_close
        %td= crypto.day_high
        %td= crypto.day_low
        %td= crypto.minute_high
        %td= crypto.minute_low
        %td= link_to 'Show', crypto
        %td= link_to 'Edit', edit_crypto_path(crypto)
        %td= link_to 'Destroy', crypto, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Crypto', new_crypto_path
