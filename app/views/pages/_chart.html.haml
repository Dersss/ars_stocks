// Open, high, low, close
.panel.stock-panel
    %h4 
        = @chosen_stock_data
    %hr
    = candlestick_chart(@chart_data.to_h, plotOptions: { candlestick: { wick: { useFillColor: true } } })

.panel.stock-panel
    %h4 WISH
    %hr
    = candlestick_chart(@chart_data2.to_h, plotOptions: { candlestick: { wick: { useFillColor: true } } })